{
  "$schema": "https://raw.githubusercontent.com/nicobailon/mcporter/main/config/mcporter.schema.json",
  "servers": {
    "example-mcp": {
      "baseUrl": "https://example-mcp-server.com",
      "auth": "oauth",
      "description": "Example OAuth MCP server (replace with actual config)"
    }
  }
}
